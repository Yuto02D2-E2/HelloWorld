void setup() {
    size(300, 300);
    background(255);
}

void draw() {
    println("hello world with processing");
    textAlign(CENTER);
    textSize(30);
    text("hello world", width / 2, height / 2);
}
